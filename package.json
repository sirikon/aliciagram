{
  "scripts": {
    "start": "ts-node-dev src/main.ts",
    "lint": "prettier --check ./src",
    "lint:fix": "prettier --write ./src",
    "build": "npm run -s lint && tsc --project ."
  },
  "dependencies": {
    "telegraf": "^4.9.2"
  },
  "devDependencies": {
    "prettier": "^2.7.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.8.3"
  }
}
